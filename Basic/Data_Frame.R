# This is similar to table where data is stored in the form of row and column
# this is also simlar to python /panda dataframe for used for analysis
user_frame<-data.frame(
  Name=c("Abhay","User1","user2"),
  Role=c("Admin","Public","Police")
)
print(user_frame)